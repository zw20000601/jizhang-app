org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
android.suppressUnsupportedCompileSdk=36

APP_VERSION_CODE=1
APP_VERSION_NAME=1.0.0

# Optional: web entry url. Empty means load bundled local assets.
WEB_APP_URL=

# Optional: external update json manifest url (effective for direct channel only).
UPDATE_MANIFEST_URL=

# Optional: release signing (required for store upload builds)
RELEASE_STORE_FILE=
RELEASE_STORE_PASSWORD=
RELEASE_KEY_ALIAS=
RELEASE_KEY_PASSWORD=
